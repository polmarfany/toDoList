<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:label="Activity principal"
    style="@style/stlMainActivity"
    android:theme="@android:style/Theme.Holo.Light.DarkActionBar"
    tools:context="com.todolist.intracloud.todolist.MainActivity">

    <Button
        android:text="Layout simple"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="48dp"
        android:id="@+id/btnLayoutSimple" />

    <Button
        android:text="Layout amb filtres y adapter personalitzat"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btnLayoutFilter"

        android:layout_below="@+id/btnLayoutSimple"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="19dp" />

    <Button
        android:text="Layout amb botons"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btnLayoutBotons"
        android:layout_marginTop="31dp"
        android:layout_below="@+id/btnLayoutFilter"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
